namespace CyberSecurityChatbot
{
    //this is the user class, it has a property called name
    //which is used to store the user's name when they enter it at the beginning of the chatbot conversation
    class User
    {
        public string Name { get; set; }
    }
}